slam_toolbox:
  ros__parameters:
    # Frame-Parameter
    odom_frame: odom
    base_frame: base_link
    scan_topic: /scan
    mode: mapping 

    use_sim_time: True

    # Mapping-Parameter
    resolution: 0.05
    max_laser_range: 20.0
    map_update_interval: 5.0